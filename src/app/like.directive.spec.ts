import { LikeDirective } from './like.directive';

describe('LikeDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeDirective();
    expect(directive).toBeTruthy();
  });
});
